package p2p

func doSyncronization(peer *peer, backend *Server) error {
	panic("not impl")
}

func (s *Server) syncReqHandle(peer *peer, req *syncReqMsg) {
	panic("not impl")
}

func (s *Server) syncResHandle(peer *peer, req *syncResMsg) {
	panic("not impl")
}
